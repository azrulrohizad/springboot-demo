# Spring Boot Batch API System

## Project Overview
This project is a Spring Boot-based backend system that processes customer records using a batch job and exposes secure RESTful APIs for data retrieval and updates.
